
day28

回顾：
    什么是socket？
        TCP：可靠的，面向连接的协议（eg：打电话），传输效率低
            使用TCP的应用：Web浏览器；电子邮件、文件传输程序。
        UDP: 不可靠的，无连接的服务，传输效率高（发送前时延小）
            使用UDP的应用：域名系统 (DNS)；视频流；IP语音(VoIP)。
今日内容：
    1.模拟ssh

    2.粘包
        只有TCP有粘包现象，UDP永远不会粘包
    3.文件上传